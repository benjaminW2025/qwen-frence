# HISTORICAL ONLY: reproduces the superseded 0.10.2 scorecard.
# New comparisons use requirements-vllm-current.txt in a separate environment.
vllm==0.10.2
# vLLM 0.10.2 only declared a lower bound, so an unconstrained install can select
# Transformers 5 and fail while initializing Qwen2Tokenizer.
transformers==4.55.2
